# you need to get into the python virtual environment:

source .venv/bin/activate

# to install a different version of esphome:

python -m pip install --upgrade --force-reinstall "esphome==2025.7.5"

# to get back out:

deactivate
